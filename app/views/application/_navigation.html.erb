<%= form_tag 'search' do |form|%>
  <%= text_field_tag :search, '', placeholder: 'Buscar...' %>
  <%= submit_tag 'Buscar' %>
<% end %>

<% if content_for? :navigation %>
  <%= yield :navigation %>
<% end %>
